export default function WorkCard({
    title,
    description,
    ready,
    link,
    github,
    image,
    content
}: {
    title: string,
    description: string,
    ready: string,
    link?: string,
    github?: string,
    image?: string,
    content: string
}) {
    return (
        <div>
            
        </div>
    )
}